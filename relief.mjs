export const clamp=(x,a=0,b=1)=>Math.max(a,Math.min(b,x));export const smooth=(a,b,x)=>{const t=clamp((x-a)/(b-a));return t*t*(3-2*t)};export function bounds(m,w,h){let x0=w,y0=h,x1=-1,y1=-1,area=0;for(let y=0;y<h;y++)for(let x=0;x<w;x++)if(m[y*w+x]>.5){x0=Math.min(x0,x);x1=Math.max(x1,x);y0=Math.min(y0,y);y1=Math.max(y1,y);area++}return area?{x0,y0,x1,y1,area,width:x1-x0+1,height:y1-y0+1}:null}export function fitMask(m,w,h,size=257){const o=new Float32Array(size*size),b=bounds(m,w,h);if(!b)return o;const s=.76*(size-1)/Math.max(b.width,b.height);for(let y=0;y<size;y++)for(let x=0;x<size;x++){const sx=Math.round((x-(size-1)/2)/s+(b.x0+b.x1)/2),sy=Math.round((y-(size-1)/2)/s+(b.y0+b.y1)/2);if(sx>=0&&sx<w&&sy>=0&&sy<h)o[y*size+x]=m[sy*w+sx]}return o}export function distance(m,w,h,inside=true){const d=new Float32Array(w*h);for(let i=0;i<d.length;i++)d[i]=(m[i]>.5)===inside?w+h:0;for(let y=0;y<h;y++)for(let x=0;x<w;x++){const i=y*w+x;if(x)d[i]=Math.min(d[i],d[i-1]+1);if(y)d[i]=Math.min(d[i],d[i-w]+1);if(x&&y)d[i]=Math.min(d[i],d[i-w-1]+Math.SQRT2);if(x<w-1&&y)d[i]=Math.min(d[i],d[i-w+1]+Math.SQRT2)}for(let y=h-1;y>=0;y--)for(let x=w-1;x>=0;x--){const i=y*w+x;if(x<w-1)d[i]=Math.min(d[i],d[i+1]+1);if(y<h-1)d[i]=Math.min(d[i],d[i+w]+1);if(x<w-1&&y<h-1)d[i]=Math.min(d[i],d[i+w+1]+Math.SQRT2);if(x&&y<h-1)d[i]=Math.min(d[i],d[i+w-1]+Math.SQRT2)}return d}function seg(x,y,ax,ay,bx,by){const dx=bx-ax,dy=by-ay,t=clamp(((x-ax)*dx+(y-ay)*dy)/(dx*dx+dy*dy||1));return Math.hypot(x-ax-t*dx,y-ay-t*dy)}export function demoMask(n=257){const m=new Float32Array(n*n),f=[[.365,.55,.325,.22,.047],[.48,.51,.48,.135,.05],[.59,.53,.625,.22,.047],[.68,.60,.757,.355,.039],[.35,.735,.225,.525,.064]];for(let y=0;y<n;y++)for(let x=0;x<n;x++){const u=x/(n-1),v=y/(n-1),p=((u-.505)/.207)**2+((v-.675)/.225)**2<1;m[y*n+x]=p||f.some(([a,b,c,d,r])=>seg(u,v,a,b,c,d)<r)?1:0}return fitMask(m,n,n,n)}const creases=[[[.25,.60],[.37,.57],[.52,.59],[.68,.63],[.79,.61]],[[.30,.64],[.40,.68],[.50,.74],[.63,.77],[.74,.77]],[[.34,.60],[.40,.68],[.42,.78],[.37,.89],[.30,.94]],[[.56,.63],[.54,.73],[.58,.83],[.53,.95]]];export function makeRelief(mask,n){const field=new Float32Array(n*n),b=bounds(mask,n,n);if(!b)return field;const di=distance(mask,n,n),do_=distance(mask,n,n,false);for(let y=0;y<n;y++)for(let x=0;x<n;x++){const i=y*n+x;if(!mask[i]&&do_[i]>6)continue;const u=(x-b.x0)/b.width,v=(y-b.y0)/b.height,edge=smooth(0,4.2*n/257,di[i]);const ga=(x,y,rx,ry)=>Math.exp(-(((u-x)/rx)**2+((v-y)/ry)**2));let line=1;for(const p of creases)for(let k=1;k<p.length;k++)line=Math.min(line,seg(u,v,...p[k-1],...p[k]));const folds=v<.49?.0035*Math.exp(-((Math.sin(v*55+u*5)/.16)**2)):0;const detail=.007*Math.exp(-((line/.008)**2))+folds,ir=Math.sin(x*.83+Math.sin(y*.37))*Math.sin(y*.65+x*.13),rim=.010*Math.exp(-((di[i]+do_[i])/(2.4*n/257))**2)*(.8+.2*ir);field[i]=-(.066+.043*ga(.34,.78,.20,.22)+.027*ga(.66,.83,.17,.18)-.018*ga(.53,.66,.16,.16))*edge+detail*edge+rim+.0012*ir*edge}return field}